#[cfg(test)]
mod tokenizer {
  use hack_compiler::classes::tokenizer::Tokenizer;

  #[test]
  fn new() {
    let input: Vec<OsString> = vec![
      "let x = 0; //amazing",
      ""]
    assert_eq!()
  }
}